# Grafana Prometheus Library -- @grafana/prometheus -- 

* ALPHA

* == componentS
  * uses
    * 👀build a Prometheus data source plugin | Grafana👀

## Installation

* ways
  * `yarn add @grafana/prometheus`, OR
  * `npm install @grafana/prometheus`
